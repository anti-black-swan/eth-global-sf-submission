pragma solidity ^0.8.13;

import {IERC20} from "../../node_modules/@openzeppelin/contracts/token/ERC20/IERC20.sol";

interface IVault {
    // function WITHDRAWAL_COOLDOWN() external view returns (uint256);
    // function MAX_WITHDRAWAL_PERCENTAGE() external view returns (uint256);
    // function token() external view returns (IERC20);
    // function userInfo(address) external view returns (uint256 balance, uint256 baseBalance, uint256 latestDeposit, uint256 latestWithdrawal);
    // function mint(address to, uint256 amount) external;
    // function burn(uint256 amount) external;
    // function deposit(uint256 amount) external;
    // function withdraw(uint256 amount) external;
}   